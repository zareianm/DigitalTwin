CREATE TABLE IF NOT EXISTS machines (
    machine_id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    name VARCHAR(255) NOT NULL,
    input_parameters VARCHAR(255) NOT NULL,
    output_parameters VARCHAR(255) NOT NULL
);